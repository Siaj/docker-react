sudo: required
services:
  - docker

before_install:
  - docker build -t sibdowsiaj/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sibdowsiaj/docker-react npm run test -- --coverage
